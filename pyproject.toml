[tool.pytest.ini_options]
testpaths = ["firmware/tests"]
pythonpath = ["firmware/src"]
addopts = "--import-mode=importlib"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W"]